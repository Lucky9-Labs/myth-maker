// Read-back inspection of the saved, standalone static candidate.
import fs from 'node:fs/promises';import assert from 'node:assert/strict';import {GLTFLoader} from 'three/addons/loaders/GLTFLoader.js';
const file=new URL('../../output/structural-loss/weak-final/effort-checkpoint.glb',import.meta.url);const b=await fs.readFile(file);const gltf=await new GLTFLoader().parseAsync(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'');const debris=gltf.scene.getObjectByName('DEBRIS_legL')??gltf.scene.getObjectByName('DEBRIS_leg.L');assert.ok(debris,'Saved candidate includes detached leg');let meshes=0;gltf.scene.traverse(n=>{if(n.isMesh)meshes++;});assert.equal(meshes,89);console.log(JSON.stringify({checkpoint:file.pathname,meshes,detachedLeg:debris.name,readBack:'passed'}));
