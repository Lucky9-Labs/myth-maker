# Animation run record

Copy into the task's existing output/docs area for sustained work; omit unused fields.

## Current state
- Intent and scope:
- Latest user correction (supersedes):
- Accepted version / candidate version:
- Native source / export hash / controller commit:
- Units, forward/up, handedness, mirrored side convention:
- Current runtime, owned PID/start identity, URL or build:
- Next concrete action:
- Blocker, if any; exact evidence needed to resume:

## Acceptance matrix
| Requested behavior and control | User-visible criterion | Physical invariant | Evidence/build | State / next action |
| --- | --- | --- | --- | --- |
| Example: forward walk | Tall stride; foot initiates before travel | Planted foot stable, no hinge twist | Pending | Inspect contact/push-off |

Use explicit states: missing, implemented, machine-checked, visually inspected, user-accepted, shipped. Do not use a single “verified” flag for all of them. Record exceptions accepted by the user and preserve the full requested scope.

## Experiment (replace after promotion; archive briefly)
- Observation and hypothesis:
- Layer changed / parameters changed:
- Comparable before/after phases and camera:
- Result, focused checks, unresolved issue:

## Checkpoint and delivery
- Protected accepted checkpoint:
- Authoritative artifact and registry disposition:
- Preview behavior versus native baked clips:
- Exact engine controls and ownership:
- PR / merge / post-merge verification when requested:
- Owned disposable resources and cleanup outcome:

Historical evidence may be reused if the relevant code/artifact is identical. Label its source version; inspect parity instead of assuming it. A process is live only after checking its handle, not because this record says so.
